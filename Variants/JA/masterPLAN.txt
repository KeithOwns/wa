1. [DONE] Create Library of AtomicScripts (AS)
    a. Created a standalone script for every state-changing action.
    b. Verified logic parity with the original core script.
    c. Synchronized UI styling across all atomic components.

2. [DONE] Create Dynamic Hub Launcher
    a. HUB_AtomicLauncher.ps1 created to dynamically scan and run AS files.
    b. Implemented professional "WinAuto" UI branding and animations.

3. Modularize Main Orchestrator (ja.ps1)
    a. Refactor ja.ps1 to be a "Thin Orchestrator" (Client).
    b. Remove all embedded state-changing code blocks.
    c. Implement a strategy (e.g. JSON config or directory scanning) to call AtomicScripts.
    d. Enable customizable suites via AmoticScript selection.
