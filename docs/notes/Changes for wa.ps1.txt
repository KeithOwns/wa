Changes for wa.ps1:

Does the script logging functions currently export any runtime errors?

Change 'Restart Notifications' to "Restart Notification"

After running the script with the NetBIOS step toggled on, the state still shows Disabled. Help me troubleshoot.

In the header, change to FGBlack on BGDarkYellow the 'ATOMIC SCRIPTS' and change ' -Configure | Maintain-' to FG DarkYellow on BG default.

Change to FGDarkRed on BG '<EXIT>'

Remove from the dashboard entirely the 
'[*] Next Steps:
1. Go back to your browser dashboard.
2. Paste the copied data directly into the paste box OR upload 'posture_audit.json' from your Desktop.'

Change the file name form 'posture_audit' to "secrutity_score"

Do not print the footer a second time after the script exits. The script should print the copyright instead.

In the footer, give the whole ' ^ v  ' part of the footer FG black on BGDarkYellow so that there are two spaces on either side of the ' ^ v ' has two spaces on the left of the '^' and two spaces on the right of the 'v' in BGDarkYellow.

Remove entirely the '=============================================
     Windows 11 Posture Audit Complete
============================================='

Change the appearance of the cursor from one ">" character to a ">" in the left margin and a "<" in the right margin of the line the cursor is selecting.

In the footer, make the footer dynamic so that when the cursor has selected the:
'Smart Run' shows "Press Enter to Smart Run"
'Manual' shows "Press Enter for Manual"

Fix the vertical alignment of the "|" characters (the Atomic Scripts column needs to be shift left by 2 spaces.

Indent the 'Running Automated System Audit Scanner...' by 4 more spaces.

While the line is not selected by the cursor, ensure that the '| Smart Run |' is indented by the same number as spaces that it is while it is selected by the cursor.

Globally change all of the FG / BG choices for all of the rest of the pages that print after the dashboard match the FG / BG choices on the dashboard.

Review the code of the scripts in the "C:\Users\admin\src\github.com\KeithOwns\as\Atomic Scripts" folder (currently locked for editing and tell me if there are any atomic scripts in the as\Atomic Scripts folder that are not assigned to a step of the wa.ps1 script. Compare the code of each one of the scripts in the "C:\Users\admin\src\github.com\KeithOwns\as\Atomic Scripts" folder to the atomic script that it matches on the wa.ps1 dashboard. 

Which hotkeys are currently are active in the script?

Which modifiers (like '-reverse') are active in the wa.ps1 script?